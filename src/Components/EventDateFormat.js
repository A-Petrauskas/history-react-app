export default EventDateFormat;

function EventDateFormat({ date }) {
    const formatedDate = date;

    // TODO: 
    //          1. Check if its just year
    //          2. If its minus change to BCE 

    return (
        formatedDate
    )
}